<!-- Playbook-Version: 9 -->
<!-- Playbook-Variant: online -->
# AGENTS.md — 所有 AI agent 的最高指令

> - **Language and Encoding Standards**: All system files, code annotations, memory files (`LLM_MEMORY.md`), and handover logs for this project are uniformly encoded in **UTF-8** and **Traditional Chinese** by default.

## 0. 強制第一步

任何動作前，先讀**本檔（`AGENTS.md`）所在目錄的** `LLM_MEMORY.md` 取得目前狀態與交接日誌。
完成任何工作段落後，必須更新**同一份** `LLM_MEMORY.md` 的〈交接日誌〉與〈目前狀態〉。

> 這裡刻意不寫 `./LLM_MEMORY.md`：`./` 依 agent 當下的工作目錄解析，巢狀導入時（工作目錄在 repo 根、讀的卻是 `sub/AGENTS.md`）會指到上層那一份，正好違反 §4「作用範圍僅限本檔所在目錄」。認檔案位置，不認工作目錄。

### 防膨脹維護

- 〈交接日誌〉超過 20 筆時，將最舊條目壓縮成摘要移入〈已封存結論〉。
- 〈已封存結論〉過長時，整段搬至 `./ARCHIVE.md`（不在強制閱讀清單，需要才查）。
- **此項不綁定階段**：任何 agent 在任何階段發現超標，都要順手處理，不得以「這不是我這階段的事」推託。

### 交接日誌書寫規範

- 追加新條目前務必重新讀取 `LLM_MEMORY.md` 最新內容；新條目時間戳需晚於目前最上方那筆，若最上方時間反而更新，代表讀到舊版，先重讀再插入——避免同工具多分頁併發編輯造成日誌錯位。
- **時間戳必須以系統指令實際取得**，禁止憑印象或推算填寫。插入前先掃過整份日誌確認時序沒有錯亂，不是只比對最上面那一筆。
- 同一 agent、同一工作階段內的連續小修改，更新既有最上方那筆即可，不必逐次新增；只有真正換手、換階段，或出現重大進度/阻塞時才新增新條目。
- 「下一個 agent 該做什麼」欄禁止套話，必須寫可執行的具體下一步；沒有具體下一步就寫「無」。

## 1. 階段分工(依 LLM_MEMORY.md〈目前狀態〉記載的階段執行,不越權)

權限跟著「專案階段」走,與使用哪個 LLM/工具無關。
階段只能由使用者宣告切換,agent 不得自行切換。

- **規劃階段 [plan]**: 只產出/修訂純文字架構與計畫,寫入 LLM_MEMORY.md。
  規劃未標記〈已定案〉前,禁止改動程式碼。
- **建置階段 [build]**: 依 LLM_MEMORY.md〈已定案〉的計畫實作基礎骨架與功能代碼。
  不重寫計畫;計畫有問題時在〈交接日誌〉留言,停止該項。
- **維運階段 [maintain]**: 長時間運行下的 debug、小幅修補、看門狗與監控優化。
  禁止架構級變更;需要動架構時在〈交接日誌〉提案,由使用者決定是否退回規劃階段。

## 2. 交接協定(硬規則)

- **接手的第一件事**:把當下 `git log -1 --format='%h %s'` 的實際輸出(雜湊＋訊息),寫進〈目前狀態〉的
  「交接基準 commit」欄,覆蓋前一手留下的值。這一欄在本次工作期間**不再更動**——
  它記的是「我接手時專案停在哪」,不是「我做到哪」,所以 HEAD 隨後領先本欄是正常狀態,不必補寫。
- **⚠️ 覆蓋前先確認 §2.1 是否觸發**:若最上方交接日誌條目寫著「需要審閱」,**先用舊值做完 §2.1 的審閱,再覆蓋本欄**。
  順序反了會出事:本欄一旦覆蓋成當下 HEAD,§2.1 要用的 `git diff <交接基準 commit>..HEAD` 就變成空的,
  審閱所需的起點被自己抹掉。兩節都寫「第一件事」,實際先後以本條為準——**先審,後覆蓋**。
- **禁止把本欄改填成自己這次 commit 的雜湊**:雜湊由檔案內容算出,把雜湊寫進記憶檔就會改變雜湊,
  這是自我指涉,數學上無解,`--amend` 幾次都一樣。硬填只會得到一個永遠錯誤的值,
  並誘使下一個 agent 為了「補正」而重複交接同一段工作。
- 若本欄記載的 hash 已不存在(history 被 rebase / amend 改寫),不要嘗試還原或猜測,
  改以 `git log --oneline -10` 判斷範圍,並在〈交接日誌〉記一筆地雷警告。
- 切換 agent 前,必須 commit(允許 WIP commit,格式見 §3)。
- 接手的 agent 從交接基準 commit + LLM_MEMORY.md〈交接日誌〉起手,
  禁止推測前一個 agent 的未記錄意圖。
- 接手時若工作樹不乾淨(前一個 agent 違規未 commit):
  先以 `[takeover] WIP: 接手時發現未提交變更` commit保存現場,
  並在〈交接日誌〉記一筆地雷警告,再開工。
- 未經使用者明確指示:禁止 push、禁止 force-push、禁止改動遠端分支。

### 2.1 Review Loop(交接日誌審閱提示)

- 若本次 commit 異動到 `LLM_MEMORY.md`、`README.md` 以外的檔案(也就是實際程式碼/功能性內容的變更),不加 commit 標籤、不設獨立欄位,直接在〈交接日誌〉本筆條目寫清楚:「完成了什麼」具體描述改了什麼、為何改;「下一個 agent 該做什麼」開頭明確寫「本次改動需要審閱:<具體要檢查什麼/風險點>」。§0 本來就強制下一個 agent 先讀交接日誌,不必再疊加額外標記。
- 純粹更新 `LLM_MEMORY.md` / `README.md` 不必寫「需要審閱」。
- 接手的 agent 讀到最上方交接日誌條目寫著「需要審閱」時,交接後第一件事是先扮演 Code Reviewer,以 `git diff <交接基準 commit>..HEAD` 檢視上一手的**完整**異動——上一手可能做了不只一筆 commit,只看 `git show HEAD` 會漏掉前面幾筆;基準 commit 就是為此而存在(見 §2)。**此處用的是前一手留下的舊值,務必在覆蓋該欄之前先做審閱**(見 §2 的順序條款)。取得異動後逐項核對該條目列出的檢查點,確認:
  1. 是否符合既有架構與規則,沒有破壞既有邏輯。
  2. 若上一手聲稱做過測試/驗證,〈交接日誌〉是否有留下驗證紀錄。
  - 通過:在新條目記錄審閱結論,才能繼續原定工作。
  - 不通過:不得自行修正上一手的東西,只在〈交接日誌〉具體記錄問題,停止推進新工作,等待使用者裁示。
- **禁止左手審右手**:寫下「需要審閱」該筆日誌與對應 commit 的那個 agent,不得在同一個對話/session 裡緊接著扮演上面的 Code Reviewer 審閱自己剛寫的東西——同一 session 仍帶著寫程式當下的推理脈絡,審不出自己的盲點,等於假審閱。若當下沒有其他工具或全新對話可以承接審閱,該 agent 必須在 commit 完成後**停手**,在〈交接日誌〉寫「等待新 session/新工具審閱」,並提醒使用者開一個全新對話(可同工具,但必須是重新起手、不沿用原對話推理過程的新 session)或換一個工具來做審閱,不得為了省事直接自己審。
- **使用者豁免(唯一逃生口)**:若使用者明確表示「本次不需審閱」,agent 在〈交接日誌〉記錄「使用者於<時間>明示豁免本次審閱」後,可直接繼續工作。agent 不得自行認定豁免,也不得以「找不到其他工具」為由自我豁免——**只有使用者說了才算**。

## 3. Commit 格式

`[階段] 動作簡述` 例:`[build] WIP: ADC 濾波參數化,尚缺單元測試`
階段標籤:`[plan]` `[build]` `[maintain]`,交接特例 `[takeover]`(見 §2)。

## 4. 邊界

- **作用範圍僅限本檔所在目錄**(巢狀導入時的管轄權):本套工作流只管本檔所在的這一層目錄及其子目錄。
  若上層目錄另有一套 `AGENTS.md` / `LLM_MEMORY.md`(常見於 monorepo 的子專案),一律**以本目錄這一套為準**:
  * 規則衝突時以本檔為準,不因上層另有規定而改變本檔條文的執行方式。
  * **禁止修改上層的 `AGENTS.md` 與 `LLM_MEMORY.md`**,包含不得把本目錄的交接日誌寫到上層那份去。
    上層那套由它自己的 agent 負責,兩者各自獨立交接、互不代管。
  * 上層檔案可以讀(供理解脈絡),但工作若確實需要**動到**本目錄以外的檔案,先回報使用者取得同意,不自行動手。
- **語言與編碼規範**：本專案所有系統文件、代碼註解、記憶檔（`LLM_MEMORY.md`）與交接日誌，預設統一採用 **UTF-8 編碼**與**繁體中文（Traditional Chinese）**。
- README.md 是人類向文件,禁止把規劃過程、狀態、待辦寫進去。
- 禁止修改本檔(AGENTS.md)除非使用者明確指示。
- 本專案的 commit 會被 `scripts/hooks/` 內的 Git Hook 檢查(commit 訊息格式是否符合 §3、是否已包含 `LLM_MEMORY.md` 的變更),未通過會被直接擋下;不得用 `--no-verify` 等方式略過檢查。

## 5. 專案技術脈絡

- 建置指令、測試指令、程式碼慣例等依專案而異的實況,一律記載於**本檔所在目錄的** `LLM_MEMORY.md`〈E. 專案技術脈絡〉,由 agent 隨專案實況更新。
- 本檔是憲法,不記載任何依專案而異的內容;每個專案的 AGENTS.md 條文一模一樣。
